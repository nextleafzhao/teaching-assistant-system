### 后端设计理念, 前后端分离语境

- **接口契约优先**
  后端与前端以结构化的接口描述文件为唯一交互依据。接口定义先行于编码实现。

- **服务端无状态**
  单次请求包含完整鉴权与上下文信息。服务器实例间不维护客户端会话状态。

- **分层职责单一**
  接入层仅处理协议适配与参数校验，领域层仅处理业务规则，持久层仅处理数据存取。层间依赖方向单向且不可逆。

- **面向消费端的数据组装**
  返回数据结构依据前端视图需求聚合，而非直接映射持久化模型。
