### 界面设计文稿.md

### 1. 设计总则与架构

本界面设计严格遵循《后台管理系统界面设计理念》，针对个体教师或小型补习班的教学管理全流程，构建高效、清晰、可预测的桌面 Web 应用界面。

- **布局铁律**：顺应眼动 F 型规律，导航固定于左侧起始区，核心操作区居中，次要功能与状态提示位于行尾。
- **色彩纪律**：整体采用中性色（灰、白）构建沉浸式工作背景。**红、绿、橙、黄仅用于状态传达**：错误（红）、成功 / 已提交（绿）、警告 / 滞后（橙 / 黄）、取消 / 失效（灰）。
- **操作闭环**：所有按钮文案采用强动词短语（如“指派计划”、“录入成绩”）。不可逆操作（如“拖拽修复”、“最终提交”）需模态二次确认。
- **减少模态中断**：非核心编辑（如学生标签修改、计划步骤打卡）采用右侧抽屉面板或行内编辑，避免弹窗阻塞视线。

### 2. 全局框架布局

**视图结构：经典三栏式后台布局**

| 区域 | 位置 | 功能描述 | 交互特性 |
| :--- | :--- | :--- | :--- |
| **侧边导航栏** | 左侧固定 | 主菜单入口（学生、日历、教学计划、成绩、资源库、备忘录、系统设置）。 | 支持折叠以扩大工作区，图标 + 文字清晰展示，层级深度 **不超过三级**。 |
| **主内容工作区** | 中间弹性区域 | 所有数据的展示与操作面板。 | 背景色 `#F5F7FA`，卡片式内容区 `#FFFFFF`，阴影轻微。 |
| **辅助 / 上下文面板** | 右侧抽屉（按需呼出） | 用于快速查看详情、行内编辑表单。 | 避免模态弹窗遮挡主任务流。 |

### 3. 核心功能界面详细设计

#### 3.1 学生与档案管理 (FR1)

**页面名称**：学生列表与详情看板

| 界面组件 | 布局位置 | 设计细节与规范 | 对应需求 |
| :--- | :--- | :--- | :--- |
| **学生列表** | 主内容区左侧 | 表格形式展示姓名、届、年级、标签缩略。支持表头排序与筛选。 | FR1.1 |
| **快捷筛选区** | 列表顶部 | 提供“届”、“年级”、“标签”的下拉多选过滤器。 | NFR3 |
| **操作按钮** | 列表右上 | **[新增学生]** (强动词)。 | FR1.1 |
| **个人详情看板** | 点击行后，右侧抽屉滑出 | **聚合视图**：顶部为基本信息卡与 **特质标签** (支持原地编辑 / 新建)。中部 Tab 切换：<br>1. **概览轴**：个人教学计划进度轴。<br>2. **成绩趋势**：近期考试成绩折线图。<br>3. **课程安排**：未来一周课时卡片。<br>4. **专属备忘录**：富文本编辑器。 | FR1.2<br>FR1.3 |

**交互反馈**：当教学计划发生滞后预警 (FR4.4) 时，学生姓名行左侧显示**黄色竖条高亮**。

#### 3.2 智能排课与日历 (FR2)

**页面名称**：可视化课表

| 界面组件 | 布局位置 | 设计细节与规范 | 对应需求 |
| :--- | :--- | :--- | :--- |
| **主视图区** | 主内容区中央 | **周视图网格**。纵轴为时间段 (07:00-22:00)，横轴为周几。 | FR2.1 |
| **资源侧边栏** | 主内容区左侧 (可收起) | 列出“我的学生”和“教学计划列表”，支持 **拖拽** 至日历格创建课程。 | FR2.2 |
| **课程卡片** | 网格内 | 显示学生姓名、计划名称缩写。卡片右上角用 **小圆点** 表示状态：灰色 (已计划)、绿色 (已完成)、划线 (已取消)。 | FR2.2 |
| **冲突提示** | 日历格内 | 当拖拽学生至已有该生课程的时段时，网格变红并显示 **“冲突：XXX 已有课”** 的 Toast 提示，阻止创建。**注：系统仅校验学生维度的冲突，允许教师在同一时间安排多名不同学生的课程。** | FR2.3 |
| **课时模板** | 顶部工具栏 | 按钮 **[应用模板]**，下拉选择预设（如“寒暑假全天”），确认后在日历生成空课程块。 | FR2.4 |

#### 3.3 极简教学资源库 (FR3)

**页面名称**：教学资料管理

| 界面组件 | 布局位置 | 设计细节与规范 | 对应需求 |
| :--- | :--- | :--- | :--- |
| **目录树** | 左侧导航区 | 展示用户添加的本地监视目录。悬停显示路径，点击刷新。 | FR3.1 |
| **文件列表** | 中央区域 | 显示当前选中目录下的文件。**失效文件**背景置灰并标记红色感叹号。 | FR3.4 |
| **记录使用抽屉** | 右侧滑出面板 | 点击 **[记录使用]** 时滑出。表单包含：**日期（支持切换上午 / 下午，默认今日）**、届、年级、类型、关联学生（多选）。**届、年级、类型字段根据上一次操作自动预填。** 点击确认后更新数据库，无模态框。 | FR3.2 |
| **悬空记录视图** | 顶部 Tab 切换 | 专门展示 `file_path` 失效的记录列表。支持拖拽某条失效记录到左侧有效文件上，弹出 **“确认合并历史记录？”** 模态确认框。 | FR3.4 |
| **使用历史追溯** | 右侧抽屉 / 详情 | 点击文件列表项，下方展开或右侧展示该文件的历史使用流水。**注意：无删除 / 修改按钮，仅展示。** | FR3.3 |

#### 3.4 教学计划与跟踪 (FR4)

**页面名称**：教学计划看板

| 界面组件 | 布局位置 | 设计细节与规范 | 对应需求 |
| :--- | :--- | :--- | :--- |
| **计划模板管理** | 顶部独立入口 | 网格视图管理模板步骤，支持拖拽步骤排序。 | FR4.1 |
| **计划指派列表** | 主内容区 | 看板形式，列显示：计划名 - 学生 - 进度条 - 状态。 | FR4.2 |
| **进度打卡交互** | 计划详情抽屉 | 步骤旁展示 **[标记完成]** 按钮及 **完成进度指示器（如 1/3）**。点击按钮可选择关联课程（下拉框）。支持批量勾选步骤同时完成。点击进度指示器可查看历史完成明细。 | FR4.3 |
| **滞后预警可视化** | 列表行 | 根据算法 (FR4.4) 计算，若滞后则 **整行背景呈现淡黄色**，并在进度条右侧显示 **“滞后 X 天”** 橙色文字。 | FR4.4 |

#### 3.5 成绩评估与录入 (FR5) - *重点效率场景*

**页面名称**：成绩录入面板

| 界面组件 | 布局位置 | 设计细节与规范 | 对应需求 |
| :--- | :--- | :--- | :--- |
| **考试配置** | 模态框 / 页面顶部 | 必须绑定知识点并分配配分 (`point_total`)。 | FR5.1 |
| **录入网格** | 模态框主体 | **高效键盘流设计**：<br>1. 每行学生包含：姓名、滑动条、分数框、[缺考] 勾选框。<br>2. 滑动条刻度为满分值，与输入框 **实时联动**。<br>3. **视觉反馈**：修改分数后行背景瞬间变为浅绿 (`#E8F5E9`)；勾选缺考变为淡黄 (`#FFF3E0`)。 | FR5.2 |
| **暂存与提交** | 模态框底部 | **[暂存草稿]** (灰色按钮) 与 **[最终提交]** (蓝色主按钮)。**注意**：只有当所有非缺考学生分数非空时，[最终提交] 按钮才由置灰变为亮起。 | FR5.2.e |
| **历史修改** | 成绩详情页 | 超过 7 天点击编辑，弹出带 **[修改原因]** 必填输入框的小弹窗。 | FR5.3 |

#### 3.6 学情分析与数据基础设施 (FR6)

**页面名称**：学情仪表盘

| 界面组件 | 布局位置 | 设计细节与规范 | 对应需求 |
| :--- | :--- | :--- | :--- |
| **知识点体系树** | 左侧设置页 | 树形结构，支持右键菜单增删改。 | FR6.1 |
| **个人学情** | 学生详情页 - 分析 Tab | **雷达图**展示各知识点掌握度 (算法见 FR6.2)。鼠标悬停显示加权计算详情。 | FR6.3.1 |
| **整体学情** | 主仪表盘 | 柱状图展示分数段分布。热力图展示班级知识点平均掌握率。 | FR6.3.2 |
| **数据导出** | **系统设置页**<br>**学情看板页** | 1. **系统设置页**：提供 **[导出完整数据库 (.db)]** 按钮，用于本地全量备份。<br>2. **学情分析面板**：在整体学情图表区域右下角，提供 **[导出 AI 分析数据集]** 按钮，触发脱敏导出向导。 | FR6.4 |

#### 3.7 通用备忘录 / 待办事项 (FR7)

**页面名称**：工作台首页 (Widget)

| 界面组件 | 布局位置 | 设计细节与规范 | 对应需求 |
| :--- | :--- | :--- | :--- |
| **待办组件** | 首页右侧 / 独立页面 | 列表区分“备忘录”与“待办”。 | FR7.1 |
| **状态色彩** | 列表项 | **红色高亮**：已逾期未完成。灰色：已完成。蓝色：关联了学生 / 课程。 | FR7.2 |
| **系统预置提醒** | 待办列表固定项 | 每周重复项“检查并备份数据”。不可删除，可标记完成。 | FR7.3 |

### 4. 非功能性需求与界面健壮性设计

| 设计原则 | 具体实现 |
| :--- | :--- |
| **离线可用与启动反馈** | 应用启动时显示 Loading Skeleton (骨架屏)，3 秒内完成加载。加载失败显示友好提示。 |
| **操作响应** | 拖拽排课时，鼠标释放瞬间即完成数据库写入并刷新视图，视觉反馈 < 100ms。 |
| **键盘效率** | 成绩录入网格支持 **Tab** 切换到下一行分数框，**Space** 勾选缺考，**Enter** 暂存。 |
| **数据防错** | 所有删除、合并、最终提交操作，均触发 **二次确认模态框**，防止误触。 |
| **事务一致性** | 成绩提交时，若网络 / 数据库异常，界面应显示“保存失败，数据已回滚”的明确错误提示。 |

### 5. 交互规范附录

- **空状态设计**：当列表无数据时，不显示空白表格，而是显示带有 **[立即创建]** 引导按钮的插画。
- **加载状态**：任何异步请求（如刷新文件列表）均采用局部 Loading 圈，不阻塞页面其他区域操作。
- **文案规范**：遵循 `操作对象 + 动词` 结构，如“学生 新增”、“成绩 提交”。

---