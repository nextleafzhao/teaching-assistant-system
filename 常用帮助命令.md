### 常用命令

`cargo --help`

`cargo help add`

`cargo help new`

`cargo doc --package 某包 --no-deps`    可能会编译几分钟，作用是生成某包的文档

**在终端中查看文档步骤（sqlx可换成其他，举一反三）：**
1. 进入项目目录    可能你已在此目录下
2. `cargo add sqlx --dry-run`    预览添加依赖后的结果，此步骤并不是每次查看都要进行
3. `cargo add sqlx --features "runtime-tokio,sqlite,macros"`    根据实际情况添加依赖，此步骤并不是每次查看都要进行
4. `cargo doc --package sqlx --no-deps`    此步骤并不是每次查看都要进行
5. `$env:PYTHONIOENCODING = 'utf-8'`或`set PYTHONIOENCODING=utf-8`    设置编码，可选的操作
6. `html2text --body-width 0 --mark-code --no-wrap-links --reference-links --pad-tables --unicode-snob 项目目录/target/doc/sqlx/某个html`    查看文档

`psql -?`

`pg_isready -?`

> 直接生成某Rust包的md文档到当前目录，可使用脚本`g生成rust依赖包的md文档.py`，用法示例：`python g生成rust依赖包的md文档.py tokio`